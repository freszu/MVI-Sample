<?xml version="1.0" encoding="utf-8"?>
<manifest package="pl.naniewicz.mvisample"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".app.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".feature.ScreensActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".TestMviActivity"/>
        <activity android:name=".feature.duck.DuckActivity"/>
        <activity android:name=".feature.login.LoginActivity"/>
    </application>

</manifest>
